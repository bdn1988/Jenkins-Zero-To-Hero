pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    docker.image('node:16-alpine').inside {
                        // Your build steps here
                        sh 'npm install'
                        sh 'npm test'
                    }
                }
            }
        }
    }
}

